# TradeGame---Preview — repo specifics

Doctrine is inherited from `~/CLAUDE.md`. This is the PUBLIC preview repo for
TradeGame (private HQ: `TradeGame`).

## Hard rules
- **Public-safe content only.** No internal strategy, no compliance/legal
  discussion, no roadmaps with internal detail, no audit notes, no member or
  owner names. When in doubt, it stays in the HQ repo.
- Owner stays unnamed (license policy).
- Education-not-financial-advice posture in every public sentence; never
  describe the project in terms of returns, profits, or signals.
- Secret scan hook active: `git config core.hooksPath .githooks` after clone.
- Branch per task; stage only files you changed.
